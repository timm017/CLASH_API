extends layout

block content
  div(id="home-main-container")
    h1 Welcome to #{title}
      div(id="home-container")
        p
          a(href="#{baseURL}topclan") Top Clans
        p
          a(href="#{baseURL}topplayers") Top Players
        p
          a(href="./leagues") Current Leagues
        p 
          input(name="clantag", type="text", placeholder="Clan Tag")
          button(id="clan-search") Search